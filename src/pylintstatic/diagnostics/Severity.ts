// Structured issue interface produced by the runner when a pylint-style
// line is parsed. Consumers can import IssueIntf to work with structured
// issue data instead of parsing text output.
type Severity = "E" | "W" | "I";

export default Severity;
